import { StyledBadge } from "./styles";
import { IoMdFlame } from "react-icons/io";

export const TrendingBadge = () => {
  return (
    <StyledBadge>
      <IoMdFlame size={20} />
    </StyledBadge>
  );
};
